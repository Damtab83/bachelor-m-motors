spring:
  application:
    name: m-motors_app
  datasource:
    url: jdbc:mariadb://localhost:3308/m-motors_app
    username: root
    password: motdepasse
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
